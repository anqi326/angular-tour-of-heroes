import { AppComponent } from './app.component';

describe('AppComponent', () => {
  it('title should equal', () => {
    const comp = new AppComponent();
    expect(comp.title).toBe('Tour of Heroes');
  });
  it('title should equal', () => {
    const comp = new AppComponent();
    expect(comp.title).toBe('Tour of Heroes');
  })
})
